{
  "name": "common",
  "version": "0.1.0",
  "private": true,
  "main": "src/lib.js",
  "types": "src/lib.d.ts",
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider craco start",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=production GENERATE_SOURCEMAP=false CI=false tsc --declaration",
    "dev": "cross-env NODE_OPTIONS=--openssl-legacy-provider tsc --declaration",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "publishWin": "xcopy .\\build ..\\..\\framework\\plugins\\lib\\ /E /I /Y",
    "publishLinux": "cp -rf ./build/* ../../framework/plugins/lib"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
